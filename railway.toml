[build]
builder = "dockerfile"

[deploy]
startCommand = "sh -lc 'openclaw doctor --fix || true; openclaw gateway run --allow-unconfigured --port 8080 --bind lan --force'"
restartPolicyType = "on_failure"

[variables]
PORT = "8080"
